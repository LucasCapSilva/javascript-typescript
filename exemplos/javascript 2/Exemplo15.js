let somar = (num1, num2) => {
    return num1 + num2;
   }
   let subtrair = (num1, num2) => {
    return num1 - num2;
   }
   
console.log(`Soma: ${somar(num1, num2)}`);
console.log(`Subtração: ${subtrair(num1, num2)}`);